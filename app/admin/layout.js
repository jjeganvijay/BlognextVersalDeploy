export default function EditLayout({ children }) {
  return (
     <div className="edit-layout-container">
      <h2 className="edit-layout-heading">✏️ Editing Blog</h2>
      {children}
    </div>
  );
}
